#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<double>v;
    int t,len;
    cin>>len;
    while(v.size()<len){
        cin>>t;
        v.push_back(t);
    }
    sort(v.begin(),v.end());
    if(v.size() % 2 == 0)
        cout<< (v[v.size()/2 - 1] + v[v.size()/2]) / 2;
    else
    cout<<v[v.size()/2];
		  
        return 0;
    
}
